import type { NextPage } from 'next';

const Dashboards: NextPage = () => {
  return (
    <div>
        <p>I am the dashboards page</p>
        <p>Create new dashboards (Data from Github, Jenkins, Jira?, Npm))</p>
        <p>Show existing dashboards</p>
    </div>
  );
};

export default Dashboards;
